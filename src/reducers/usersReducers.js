"use strict";

// USERS REDUCERS
const initialState = {};
export function usersReducers(state = initialState, action) {
    switch (action.type) {
        default:
            return { ...state };
            break;
    }

    return state;
}
